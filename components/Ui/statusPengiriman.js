import classes from './statusPengiriman.module.css';

export default function StatusPengiriman() {
  return (
    <div className={`card ${classes.titleCard}`}>
      <h2 className={classes.title}>Status Pengiriman</h2>
    </div>
  );
}
